// Crie uma função que recebe um nome e retorna:
// "Olá, [nome]!"

let nome = 'duda'
const saudacao = (nome) => {
    return `Olá, ${nome}!`;
}

console.log(saudacao(nome))

const dobro = function(n) {
  return n * 2;
};

const triplo = (n) => n * 3;
